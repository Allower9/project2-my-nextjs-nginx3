version: '3.8'

services:
  nginx:
    image: allower99/project2-nginx-frontend:latest
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - frontend-net

networks:
  frontend-net:
    driver: bridge
